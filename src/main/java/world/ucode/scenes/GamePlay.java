package world.ucode.scenes;

import javafx.stage.Stage;

public class GamePlay extends GameScene {
    GamePlay(Stage primaryStage, String fxmlName, Object Controller) {
        super(primaryStage, fxmlName, Controller);
    }
}
